// Colours
$mainColor = #00A388
$navColor = white
$navBgColor = lighten($mainColor, 20%)
$contentBgColor = #FCFFF5
$linkColor = lighten($mainColor, 20%)

// Sizes
$largeScreenThreshold = 1700px

// Typography
$baseFontSize = 16px
$baseFontSizeWide = 18px
$h1FontSize = 2 * $baseFontSize
$h2FontSize = 1.5 * $baseFontSize
$h3FontSize = 1.5 * $baseFontSize
$footerFontSize = 0.8 * $baseFontSize